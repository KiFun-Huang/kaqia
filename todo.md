# 优先级1
- [x] 箭头现在生成了，但是位置不对。
- [x] 文字还是出现无限生成。鼠标左键任何操作都被阻塞。
- [x] 所有颜色预设值需要固定，现在出现的情况就是每次更新颜色都会变一点，并且要支持自定义颜色的使用。
- [x] readme 的logo有点大。


# 优先级2（大版本，先不改）
- [ ] 工具栏需要可拖拽，而且工具栏出现的时候要判断是否超出屏幕范围，现在是会出现超过屏幕范围。
- [ ] 截图的框选框，在编辑前可移动可左右拖拽调整宽度，可上下拖拽调整高度。框选时要显示 (x,y)坐标。